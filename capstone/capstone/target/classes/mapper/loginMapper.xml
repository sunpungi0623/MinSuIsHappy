<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mapper.LoginMapper">

<!-- 로그인 하기 -->
	<select id="login" resultType="domain.LoginVO">
		select *
		from User
		where id=#{id}
		and password=#{password}
	</select>
	
	<insert id="SignUp" parameterType="domain.LoginVO">
		insert into User(id, password, userName, userPhone, Type) values(#{id},#{password},#{userName},#{userPhone},#{Type});
	</insert>
</mapper>